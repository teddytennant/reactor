# Copy to .env and fill in. Never commit .env.

DAYTONA_API_KEY=
DAYTONA_API_URL=

FIREWORKS_API_KEY=

# Analyst: reasons over evidence and writes the verdict (needs tool calling).
ANALYST_MODEL=
# Victim: sacrificial agent — cheap, tool-call capable.
VICTIM_MODEL=
REACTOR_VICTIM_BACKEND=
